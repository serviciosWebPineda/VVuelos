@model IEnumerable<V_Vuelo.Models.Aerolinea>

@{
    ViewData["Title"] = "Aerolineas";
}

<h1>Aerolineas</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre_aerolinea)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_aerolinea)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Aerolinea_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Aerolinea_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Aerolinea_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
